#include <stdio.h>
//reverse_number
int main()
{
    int num,rem,rev=0;
    printf("Enter a number!\n");
    scanf("%d",&num);
    while(num>=1){
        rem=num%10;
        rev=rev*10+rem;
        num=num/10;
    }
    printf("Reversed number is %d",rev);
    
   
    return 0;
}
